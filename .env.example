# Discord Developer Portal -> sua aplicação -> OAuth2
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Gere com: openssl rand -base64 32
NEXTAUTH_SECRET=
# Em produção, use a URL real do seu site (ex: https://harumi.vercel.app)
NEXTAUTH_URL=http://localhost:3000

# String de conexão do PostgreSQL (Neon, Vercel Postgres, Supabase, etc.)
DATABASE_URL=
